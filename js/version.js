// Mostrado em Configuracoes so para confirmar, depois de um deploy, que o
// aparelho realmente pegou a versao nova (o app nao tem build step, entao
// isto e incrementado a mao a cada publicacao - ver README).
export const APP_VERSION = 'v8';
